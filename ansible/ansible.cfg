[defaults]
inventory = inventory.ini
# Routers get reflashed and their host keys churn; pinning them would break
# every re-provision. LAN-only management traffic, accepted risk.
host_key_checking = False
retry_files_enabled = False
